<UserControl x:Class="EP_HSRlearnIT.PresentationLayer.MenuTile"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Border Name="TileBorder" Style="{StaticResource TileBorderStyle}">
            <Grid Background="{StaticResource TileBackgroundBrush}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>
                <Image Grid.Row="1" Name="TileImage" Stretch="Uniform"/>
                <TextBlock Grid.Row="2" Name="TileText" Style="{StaticResource TileContentStyle}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
