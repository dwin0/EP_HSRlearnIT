<Page x:Class="EP_HSRlearnIT.PresentationLayer.Games.DragDropPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d" 
      Title="DragDrop">

    <Viewbox HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Canvas Name="ElementCanvas" Height="720" Width="960" MouseMove="ElementCanvas_MouseMove">
            <Rectangle Height="600" Width="700" Margin="-50, 50, 0, 0">
                <Rectangle.Fill>
                    <ImageBrush ImageSource ="/Images/dragdrop.PNG"/>
                </Rectangle.Fill>
            </Rectangle>

            <Button Name="Reset" Content="Reset" Click="OnResetClick" Height="40" Width="90" Canvas.Left="800" Canvas.Bottom="30"/>

            <TextBox Name="GameInstruction" Text="{StaticResource ResourceKey=GameInstruction}" FontSize="15" IsReadOnly="True" Background="AliceBlue" Foreground="black" BorderBrush="Blue" BorderThickness="2" Width="750" Height="350" Canvas.Left="-50" Canvas.Top="55" TextWrapping="Wrap"/>

            <Button Name="ButtonCloseGameInstruction" Content="Schliessen" Canvas.Top="330" Canvas.Left="600" Width="80" Height="35" Click="CloseGameInstruction" />

            <Button Name="Check"  Height="40" Width="90" Content="Resultat" Canvas.Left="700" Canvas.Bottom="30" Click="Check_OnClick" IsEnabled="False"/>

            <StackPanel>
                <Button Name="BtnOpen" HorizontalAlignment ="Left" Width="100" Margin="580,670,0,30" Content="Spielanleitung" />
                <Popup Name="Popup" PlacementTarget="{Binding ElementName=BtnOpen}" AllowsTransparency="True" >
                    <Grid Width="200" Height="200" Background="White" Margin="150">
                        <Button Name="BtnClose" Content="Popup schließen"/>
                    </Grid>
                </Popup>
            </StackPanel>

        </Canvas>
    </Viewbox>
</Page>
